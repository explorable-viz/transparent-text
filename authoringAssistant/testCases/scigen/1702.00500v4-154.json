{
  "datasets": [
    {
      "var": "tableData",
      "file": "datasets\/1702.00500v4-154"
    }
  ],
  "imports": [],
  "variables": {},
  "expected": {
    "result1": "foldr (fun x acc -> if x.test < acc then x.test else acc) (head (map (fun x -> x.test) tableData)) (map (fun x -> x.test) tableData)"
  },
  "paragraph": [
    {
      "type": "literal",
      "value": "Over the rules used on the 1-best result, more than [REPLACE id=\"result1\" value=\"?\"]% are non-terminal rules,  On the other hand, 30% are glue rules.  Finally, terminal rules take the largest percentage."
    }
  ]
}
